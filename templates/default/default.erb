server {
	listen 80 default_server;
	listen [::]:80 default_server;

  # underscore is an invalid server_name that will never trigger a real hostname
	server_name _;

  location / {
    include uwsgi_params;
    uwsgi_pass 127.0.0.1:3031;
    uwsgi_modifier1 5;
  }

}